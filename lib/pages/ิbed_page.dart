import 'package:flutter/material.dart';

class BedPage extends StatelessWidget {
  const BedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bed Page'), // ตั้งชื่อแสดงบน AppBar ตามที่คุณต้องการ
      ),
      body: Container(), // ส่วนเนื้อหาของหน้า BedPage
    );
  }
}
