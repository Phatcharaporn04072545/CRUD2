import 'package:flutter/material.dart';

class BloodSugar extends StatelessWidget {
  const BloodSugar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Blood Sugar'), // ตั้งชื่อแสดงบน AppBar ตามที่คุณต้องการ
      ),
      body: Container(), // ส่วนเนื้อหาของหน้า BloodSugar
    );
  }
}
